<div class="container-fluid">
  <!-- Added data-player-name to see if player.username is being passed to JS-->
  <div id="instance" data-instance-id="<%=@instance.id%>" data-host-id="<%=@instance.user_id%>" data-host-name="<%=@instance.user.username%>" data-player-name="<%=@each_player_name%>" data-player-id="<%=@each_player_id%>">

  <!-- Players Waiting page -->
  <div id="waiting-page">
    <%= render "show_waiting", players: @players, instance: @instance %>
    <%= simple_form_for [@instance, @player_inputs] do |f| %>
    <!-- Implement logic/condition so than only Host should see/use this -->
      <%= f.submit "Start Game", class:"btn btn-secondary" %>
    <% end %>
  </div>
  </div>
</div>
